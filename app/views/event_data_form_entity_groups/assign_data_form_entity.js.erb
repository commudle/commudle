{

 function addAssignedForm(){
     let newAssignedFormListItem = $('#event-data-form-entities-<%= @edfeg.id %>').append("<%= escape_javascript render 'assigned_form_list_item', dfe: @dfe, event: @event %>");


     let notification = new Noty({
         text: 'New Form Assigned',
         theme: 'relax',
         type: 'success'
     }).show();
 }


 addAssignedForm();

}


