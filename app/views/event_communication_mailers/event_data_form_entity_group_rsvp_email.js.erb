{
    let form = $("<%= escape_javascript render 'event_data_form_entity_group_rsvp_email_form' %>");

    let modal = $('#send-mails-modal');

    $(' .modal-title', modal).html("Send RSVP Email");

    $(' .modal-body', modal).empty().append(form);

    tinyMCE.init({
        selector: '.tinymce.editor'
    });

    modal.modal('show');

}