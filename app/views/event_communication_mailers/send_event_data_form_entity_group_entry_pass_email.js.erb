{
    $('#send-mails-modal').modal('hide');
    let notification = new Noty({
        text: 'Emails are being delivered, hold on!',
        theme: 'relax',
        type: 'success',
        timeout: 1500
    }).show();
}