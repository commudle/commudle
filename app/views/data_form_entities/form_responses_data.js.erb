{
  <% @responses.each.with_index((@count * (@page - 1)) + 1) do |response, index| %>

    $('#form-responses-container').append($('<%= escape_javascript render "data_form_entities/form_response_row", dferg: response, index: index %>'));

  <% end %>
  localStorage.page = parseInt(localStorage.page) + 1;

  if('<%= @pending_data %>' != 'true'){
      pendingData = false;
  }
}