{
    $('#mark-attendance-form').trigger("reset");
    $('#mark-attendance-form input[type=number]').focus();
    let notification = new Noty({
        text: '<% if !@already_marked %>Welcome to the Event <strong><%= @entry_pass.user.name %></strong>! Have a Great Day Ahead! <% else %> Already Marked Your Visit! <% end %>',
        theme: 'relax',
        type: '<% if !@already_marked %>success<% else %>error<% end %>',
        timeout: 2500
    }).show();
}