{

    $('#event-status-select').replaceWith($("<%= escape_javascript render 'event_status_select', event: @event, event_statuses: @event_statuses %>"));


    let notification = new Noty({
        text: 'Status Updated!',
        theme: 'relax',
        type: 'success'
    }).show();
}