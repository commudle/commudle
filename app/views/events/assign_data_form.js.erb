{

 function addAssignedForm(){
     let newAssignedFormListItem = $('#assigned-forms-list').append("<%= escape_javascript render 'assigned_form_list_item', dfe: @dfe, event: @event %>");


     let notification = new Noty({
         text: 'New Form Assigned',
         theme: 'relax',
         type: 'success'
     }).show();
 }


 addAssignedForm();

}


