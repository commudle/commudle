{
  let newSlots = $("<%= escape_javascript render 'event_location_tracks/track_slots', event_location_track: @track_slot.event_location_track %>");

  $('#event-location-track-slots-<%= @track_slot.event_location_track_id %>').empty().append(newSlots);
  $('#track-slot-form-<%= @track_slot.event_location_track_id %>').trigger('reset');

    let notification = new Noty({
        text: 'Slot Created',
        theme: 'relax',
        type: 'success',
        timeout: 1500
    }).show();
}