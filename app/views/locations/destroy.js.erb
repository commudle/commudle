{
    $('#event-location-<%= @location.id %>').remove();
    let notification = new Noty({
        text: 'Location Removed!',
        theme: 'relax',
        type: 'warning',
        timeout: 1500
    }).show();
}